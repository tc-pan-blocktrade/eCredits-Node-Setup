# Never commit validator keys
eth2_scripts/gened/

# Never commit environment variables
full_nodes/.env
*.env

# Never commit passwords or jwt secrets (these are outside repo, but just in case)
var/lib/eSync/mainnet/password.cfg
var/lib/eSync/mainnet/jwt.mainnet.hex

# Never commit Zone.Identifier files
*.Zone.Identifier
